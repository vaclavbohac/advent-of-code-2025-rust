# Advent of Code 2025 - in Rust

The advent of code tasks serve as a tool to learn Rust language basics.

Claude is used only as a feedback tool to write Rust idiomatically.

Each advent of code day is runnable by running

```sh
cat resources/input.txt | cargo run
```
